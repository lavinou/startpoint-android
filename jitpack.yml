install:
  - jdk:
    - openjdk17
  - ./gradlew clean
build:
  - ./gradlew build --stacktrace --info  # Builds the project
test:
  - ./gradlew testDebugUnitTest --stacktrace  # Unit tests for debug build
publish:
  - ./publishToMavenLocal